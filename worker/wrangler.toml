name = "matrimonial-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# -- Set ALL secrets via: wrangler secret put SECRET_NAME ---------------------
# FAST2SMS_API_KEY       — fast2sms.com dashboard ? Dev API
# FIREBASE_PROJECT_ID    — Firebase project ID
# FIREBASE_CLIENT_EMAIL  — from service account JSON
# FIREBASE_PRIVATE_KEY   — from service account JSON (full RSA key)
#
# CALLMEBOT_API_KEY      — send WhatsApp to +34 644 52 51 50 saying
#                          "I allow callmebot to send me messages"
#                          they reply with your key
# ADMIN_WHATSAPP         — your WhatsApp number with country code, no +
#                          e.g. 919876543210
#
# RESEND_API_KEY         — resend.com ? API Keys ? Create Key (free)
# APP_FROM_EMAIL         — e.g. noreply@yourmatrimonialdomain.in
#                          (must be a domain you verify on Resend)
